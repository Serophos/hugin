# Version 1.1
## Bug Fixes
- Header Tabbar in Canvas editor is broken
- Field assignement in Canvas editor is brokeb

## Design 
- Unify Admin Area: The location, group and display setup forms use a different flow then the rest of hugin

## Features
- Allow setting a Language for each display (System / German / English - default to system)
- Improve Startup behaviour. Cache media during first loading screen. Then The server should orchestrate the slideshow starting behaviour for display groups
- Extend Display Groups:
- Group editor: Improve Grid Layout editor for monitor placements. Adding displays should not overlap existing displays, and add snapping behavior
- Group Editor, add possibility to set primary display (for later use)
- Add possibility to conenct to displays with VNC (if the display supports it, configurable in display setup)
- Add possibility to connect to displays with SSH (if the display supports it, configurable in display setup)



# Version 1.2
## Bug Fixes

## Design

## Features
- Add Displaygroup Playlist as a new playlist type, where Slides can move around the screens in a group with transistion effects.
